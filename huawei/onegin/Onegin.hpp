/*! 
 * \brief This function takes a file, gets all the text from it and than sorts it
 */
int Onegin (const char* FileFrom, const char* FileTo);

/*! 
 * \brief This function take output of \ref Onegin function and checks it with comparator
 */
int testoutput(char* FileName);